# newsarchiver
A locally hosted web service that accepts a news article URL and returns a self-contained, paywall-bypassing HTML snapshot of that article — functionally equivalent to what archive.is produces, but running on your own machine.
